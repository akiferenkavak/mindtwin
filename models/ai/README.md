# AI Models

This folder is reserved for ML/DL-based models (e.g., autoencoders, LSTM, transformers).

Suggested structure:
- `train_*.py` for training scripts
- `*_detector.py` for inference wrappers
- optional subfolders for experiments

Keep statistical baselines in `models/stats/`.
